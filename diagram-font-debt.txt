# Diagrams below the readability floor. Read by scripts/check_diagram_fonts.py.
#
# This file may only shrink. An unlisted violation fails the gate, and so does a listed file that
# now meets the floor -- which is what forces a line out when a diagram is fixed. Adding a line is
# possible but never quiet: it lands in the diff beside the diagram that needed it.
#
# All of these predate the gate: fontSize 11 and 12 on canvases between 1080 and 1300px, so a
# reader's 880px column receives them at roughly 7.5 to 9px.
#
# Fixing them needs one decision that is not a layout decision: whether each figure's in-image
# notes box moves into the surrounding prose. Kept in the figure, its longest line dictates the
# canvas width, and a wider canvas raises the required font again (1238px -> 20). Moved out, what
# remains is relayout -- fold labels to two lines, narrow the canvas toward 880px, stack vertically.
#
# Standard: ~/.kiro/steering/global-document-readability.md
docs/diagrams/architecture-overview-en.drawio
docs/diagrams/architecture-overview.drawio
docs/diagrams/pattern-01-edge-ai-bedrock-en.drawio
docs/diagrams/pattern-01-edge-ai-bedrock.drawio
docs/diagrams/pattern-05-agentic-rag-en.drawio
docs/diagrams/pattern-05-agentic-rag.drawio
